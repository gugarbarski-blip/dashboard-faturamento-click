# Copie para .env.local e preencha com os dados do seu projeto Supabase.
# Sem esses valores, o sistema roda em "modo demonstração" (dados ficam só no navegador).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
